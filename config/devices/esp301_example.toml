extends = "bases/newport_motion_base.toml"
schema_version = 3

[capabilities.movable]

[commands.home]
description = "Home the axis"
expects_response = false
template = "OR{{ axis }}"

[connection]
baud_rate = 19200
timeout_ms = 5000
type = "serial"

[device]
capabilities = ["Movable"]
description = "Newport ESP301 3-axis motion controller"
model = "ESP301"
name = "Newport ESP301"

[ui]
panel_kind = "stage"
