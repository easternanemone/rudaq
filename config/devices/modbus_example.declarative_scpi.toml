# NOTE: Modbus RTU is a binary protocol; v2 declarative SCPI driver is ASCII-only.
# This placeholder manifest is disabled until binary protocol support is added.

schema_version = 2
name = "Modbus Example Device"
version = "1.0.0"

[connection]
type = "serial"
baud_rate = 9600
terminator = "\n"
timeout_ms = 1000

[commands]

[capabilities]

[[instances]]
id = "modbus_example"
name = "Modbus Example Device"
port = "/dev/ttyUSB0"
address = "1"
disable = true
