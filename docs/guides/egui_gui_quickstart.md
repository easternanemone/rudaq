## rust-daq egui GUI Quickstart (`rust_daq_gui_egui`)

The egui GUI quickstart has been consolidated into the main GUI guide.

For up‑to‑date, step‑by‑step instructions on:

- Starting the daemon with networking,
- Building and running `rust_daq_gui_egui`,
- Connecting to the daemon and refreshing devices,
- Using the device table and extending the GUI,

see **`docs/guides/rust-daq-gui-guide.md`**.

This file is retained only as a pointer to avoid duplicate content.


